# --- API backend (FastAPI + SSE streaming) ---
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0

# --- auth + database (SQLAlchemy: Postgres in prod, SQLite in dev) ---
SQLAlchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9      # Postgres driver (only used when DATABASE_URL is postgres)
PyJWT>=2.8                # JWT access/refresh tokens
bcrypt>=4.0               # password hashing

# --- RAG app + pipeline ---
streamlit>=1.35          # optional lightweight alternative UI (streamlit run app.py)
langgraph>=0.2
langchain>=0.2
langchain-community>=0.2
langchain-openai>=0.1
langchain-text-splitters>=0.2
chromadb>=0.5
sentence-transformers>=3.0
rank-bm25>=0.2.2
tiktoken>=0.7
python-dotenv>=1.0
requests>=2.31
ddgs>=9.0                # keyless internet search (DuckDuckGo) for the search tool

# --- multimodal ingestion ---
pymupdf>=1.24          # PDF text + native table extraction + page rendering
pdfplumber>=0.11       # table fallback
beautifulsoup4>=4.12   # HTML (SEC filings) parsing
lxml>=5.0              # fast HTML/table parser used by pandas.read_html
pandas>=2.0

# --- optional: multimodal file upload (images / audio / docx), auto-detected ---
# Images (OCR) — install ONE:
# pytesseract>=0.3.10          # also: brew install tesseract
# rapidocr-onnxruntime>=1.3    # pip-only, no system deps
# Audio (transcription):
# faster-whisper>=1.0          # CPU int8, fast; or: openai-whisper
# Documents:
# python-docx>=1.1             # .docx text extraction
# pillow>=10.0                 # image handling (usually already present via torch)
