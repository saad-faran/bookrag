# --- API backend (FastAPI + SSE streaming) ---
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0

# --- RAG app + pipeline ---
streamlit>=1.35          # optional lightweight alternative UI (streamlit run app.py)
langgraph>=0.2
langchain>=0.2
langchain-community>=0.2
langchain-openai>=0.1
langchain-text-splitters>=0.2
chromadb>=0.5
sentence-transformers>=3.0
rank-bm25>=0.2.2
tiktoken>=0.7
python-dotenv>=1.0
requests>=2.31

# --- multimodal ingestion ---
pymupdf>=1.24          # PDF text + native table extraction + page rendering
pdfplumber>=0.11       # table fallback
beautifulsoup4>=4.12   # HTML (SEC filings) parsing
lxml>=5.0              # fast HTML/table parser used by pandas.read_html
pandas>=2.0

# --- optional OCR for scanned PDFs (auto-detected; install ONE if you add scans) ---
# pytesseract>=0.3.10  # also: brew install tesseract
# rapidocr-onnxruntime>=1.3  # pip-only, no system deps
